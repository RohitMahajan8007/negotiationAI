{
  "name": "ai-negotiation-game",
  "version": "1.0.0",
  "description": "Elite AI-Powered Merchant Marketplace",
  "main": "backend/src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm install --prefix frontend && npm run build --prefix frontend && npm install --prefix backend && mkdir -p backend/public && cp -rv frontend/dist/* backend/public/",
    "start": "node backend/src/index.js",
    "install:all": "npm install --prefix frontend && npm install --prefix backend"
  },
  "keywords": [
    "ai",
    "negotiation",
    "mern",
    "gemini"
  ],
  "author": "Rohit Mahajan",
  "license": "ISC"
}
